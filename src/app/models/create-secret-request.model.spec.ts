import { CreateSecretRequest } from './create-secret-request.model';

describe('CreateSecretRequest', () => {
  it('should create an instance', () => {
    expect(new CreateSecretRequest()).toBeTruthy();
  });
});
