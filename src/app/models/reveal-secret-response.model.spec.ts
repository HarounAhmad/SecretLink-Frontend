import { RevealSecretResponse } from './reveal-secret-response.model';

describe('RevealSecretResponse', () => {
  it('should create an instance', () => {
    expect(new RevealSecretResponse()).toBeTruthy();
  });
});
